/**
 * Base Service class representing the core business logic layer.
 * Concrete services extend this to share common lifecycle hooks or utilities.
 */
export abstract class BaseService {
  // Common helper methods can be added here as the application scales.
}
